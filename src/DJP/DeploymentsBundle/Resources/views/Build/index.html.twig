{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Build list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Vcsrevisionid</th>
                <th>Vcsbranchid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('build_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.vcsRevisionId }}</td>
                <td>{{ entity.vcsBranchId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('build_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('build_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('build_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
